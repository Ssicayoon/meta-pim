domain: "{ S0[i, j, k] : 0 <= i < 256 and 0 <= j < 256 and 0 <= k < 256 }"
accesses:
  - S0:
      reads:
        - "{ S0[i, j, k] -> A[i, k] }"
        - "{ S0[i, j, k] -> B[k, j] }"
      writes:
        - "{ S0[i, j, k] -> C[i, j] }"
